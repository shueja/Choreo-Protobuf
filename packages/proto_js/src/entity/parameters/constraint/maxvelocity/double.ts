// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: entity/parameters/constraint/maxvelocity/double.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface DoubleMaxVelocity {
  max: number;
}

export interface TestDouble {
  test: string;
}

function createBaseDoubleMaxVelocity(): DoubleMaxVelocity {
  return { max: 0 };
}

export const DoubleMaxVelocity: MessageFns<DoubleMaxVelocity> = {
  encode(message: DoubleMaxVelocity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max !== 0) {
      writer.uint32(9).double(message.max);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoubleMaxVelocity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoubleMaxVelocity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.max = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoubleMaxVelocity {
    return { max: isSet(object.max) ? globalThis.Number(object.max) : 0 };
  },

  toJSON(message: DoubleMaxVelocity): unknown {
    const obj: any = {};
    if (message.max !== 0) {
      obj.max = message.max;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DoubleMaxVelocity>, I>>(base?: I): DoubleMaxVelocity {
    return DoubleMaxVelocity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DoubleMaxVelocity>, I>>(object: I): DoubleMaxVelocity {
    const message = createBaseDoubleMaxVelocity();
    message.max = object.max ?? 0;
    return message;
  },
};

function createBaseTestDouble(): TestDouble {
  return { test: "" };
}

export const TestDouble: MessageFns<TestDouble> = {
  encode(message: TestDouble, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.test !== "") {
      writer.uint32(10).string(message.test);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestDouble {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestDouble();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.test = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestDouble {
    return { test: isSet(object.test) ? globalThis.String(object.test) : "" };
  },

  toJSON(message: TestDouble): unknown {
    const obj: any = {};
    if (message.test !== "") {
      obj.test = message.test;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TestDouble>, I>>(base?: I): TestDouble {
    return TestDouble.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestDouble>, I>>(object: I): TestDouble {
    const message = createBaseTestDouble();
    message.test = object.test ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
