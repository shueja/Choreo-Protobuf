// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: entity/trajectory.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DifferentialSample } from "./differential_sample";
import { DoubleRobotConfig } from "./parameters/robotconfig/double";
import { SwerveSample } from "./swerve_sample";

export interface SwerveTrajectory {
  samples: SwerveSample[];
}

export interface DifferentialTrajectory {
  samples: DifferentialSample[];
}

export interface GenerationOutput {
  trajectory?: { $case: "swerve"; swerve: SwerveTrajectory } | {
    $case: "differential";
    differential: DifferentialTrajectory;
  } | undefined;
  splits: number[];
  waypoints: number[];
  config: DoubleRobotConfig | undefined;
}

function createBaseSwerveTrajectory(): SwerveTrajectory {
  return { samples: [] };
}

export const SwerveTrajectory: MessageFns<SwerveTrajectory> = {
  encode(message: SwerveTrajectory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.samples) {
      SwerveSample.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SwerveTrajectory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSwerveTrajectory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.samples.push(SwerveSample.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SwerveTrajectory {
    return {
      samples: globalThis.Array.isArray(object?.samples)
        ? object.samples.map((e: any) => SwerveSample.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SwerveTrajectory): unknown {
    const obj: any = {};
    if (message.samples?.length) {
      obj.samples = message.samples.map((e) => SwerveSample.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SwerveTrajectory>, I>>(base?: I): SwerveTrajectory {
    return SwerveTrajectory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SwerveTrajectory>, I>>(object: I): SwerveTrajectory {
    const message = createBaseSwerveTrajectory();
    message.samples = object.samples?.map((e) => SwerveSample.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDifferentialTrajectory(): DifferentialTrajectory {
  return { samples: [] };
}

export const DifferentialTrajectory: MessageFns<DifferentialTrajectory> = {
  encode(message: DifferentialTrajectory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.samples) {
      DifferentialSample.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DifferentialTrajectory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDifferentialTrajectory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.samples.push(DifferentialSample.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DifferentialTrajectory {
    return {
      samples: globalThis.Array.isArray(object?.samples)
        ? object.samples.map((e: any) => DifferentialSample.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DifferentialTrajectory): unknown {
    const obj: any = {};
    if (message.samples?.length) {
      obj.samples = message.samples.map((e) => DifferentialSample.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DifferentialTrajectory>, I>>(base?: I): DifferentialTrajectory {
    return DifferentialTrajectory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DifferentialTrajectory>, I>>(object: I): DifferentialTrajectory {
    const message = createBaseDifferentialTrajectory();
    message.samples = object.samples?.map((e) => DifferentialSample.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGenerationOutput(): GenerationOutput {
  return { trajectory: undefined, splits: [], waypoints: [], config: undefined };
}

export const GenerationOutput: MessageFns<GenerationOutput> = {
  encode(message: GenerationOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.trajectory?.$case) {
      case "swerve":
        SwerveTrajectory.encode(message.trajectory.swerve, writer.uint32(10).fork()).join();
        break;
      case "differential":
        DifferentialTrajectory.encode(message.trajectory.differential, writer.uint32(18).fork()).join();
        break;
    }
    writer.uint32(26).fork();
    for (const v of message.splits) {
      writer.uint64(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.waypoints) {
      writer.double(v);
    }
    writer.join();
    if (message.config !== undefined) {
      DoubleRobotConfig.encode(message.config, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerationOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerationOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trajectory = { $case: "swerve", swerve: SwerveTrajectory.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.trajectory = {
            $case: "differential",
            differential: DifferentialTrajectory.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.splits.push(longToNumber(reader.uint64()));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.splits.push(longToNumber(reader.uint64()));
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 33) {
            message.waypoints.push(reader.double());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.waypoints.push(reader.double());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.config = DoubleRobotConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerationOutput {
    return {
      trajectory: isSet(object.swerve)
        ? { $case: "swerve", swerve: SwerveTrajectory.fromJSON(object.swerve) }
        : isSet(object.differential)
        ? { $case: "differential", differential: DifferentialTrajectory.fromJSON(object.differential) }
        : undefined,
      splits: globalThis.Array.isArray(object?.splits) ? object.splits.map((e: any) => globalThis.Number(e)) : [],
      waypoints: globalThis.Array.isArray(object?.waypoints)
        ? object.waypoints.map((e: any) => globalThis.Number(e))
        : [],
      config: isSet(object.config) ? DoubleRobotConfig.fromJSON(object.config) : undefined,
    };
  },

  toJSON(message: GenerationOutput): unknown {
    const obj: any = {};
    if (message.trajectory?.$case === "swerve") {
      obj.swerve = SwerveTrajectory.toJSON(message.trajectory.swerve);
    } else if (message.trajectory?.$case === "differential") {
      obj.differential = DifferentialTrajectory.toJSON(message.trajectory.differential);
    }
    if (message.splits?.length) {
      obj.splits = message.splits.map((e) => Math.round(e));
    }
    if (message.waypoints?.length) {
      obj.waypoints = message.waypoints;
    }
    if (message.config !== undefined) {
      obj.config = DoubleRobotConfig.toJSON(message.config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerationOutput>, I>>(base?: I): GenerationOutput {
    return GenerationOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerationOutput>, I>>(object: I): GenerationOutput {
    const message = createBaseGenerationOutput();
    switch (object.trajectory?.$case) {
      case "swerve": {
        if (object.trajectory?.swerve !== undefined && object.trajectory?.swerve !== null) {
          message.trajectory = { $case: "swerve", swerve: SwerveTrajectory.fromPartial(object.trajectory.swerve) };
        }
        break;
      }
      case "differential": {
        if (object.trajectory?.differential !== undefined && object.trajectory?.differential !== null) {
          message.trajectory = {
            $case: "differential",
            differential: DifferentialTrajectory.fromPartial(object.trajectory.differential),
          };
        }
        break;
      }
    }
    message.splits = object.splits?.map((e) => e) || [];
    message.waypoints = object.waypoints?.map((e) => e) || [];
    message.config = (object.config !== undefined && object.config !== null)
      ? DoubleRobotConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
